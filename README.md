# Raspberry Pi Pico W based led controller
This project makes use of a chinese Pico W copy, and a custom built board, which drives a 12V volt LED strip.
The communication uses the MQTT protocol, to change the LED strip's colour.
